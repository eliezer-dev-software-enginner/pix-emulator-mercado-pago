# URL do seu webhook (onde o emulador vai notificar)
WEBHOOK_URL=http://localhost:3000/api/checkout/webhook

# Porta do emulador
PORT=3001

# Delay em ms antes de disparar o webhook após criar o pagamento
WEBHOOK_DELAY_MS=2000
